# Core data & ML
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
sqlalchemy>=2.0.0

# Dashboard & visualisation
streamlit>=1.32.0
plotly>=5.19.0

# AI / LLM providers (choose one or more)
openai>=1.12.0          # optional — needed only if LLM_PROVIDER=openai
anthropic>=0.21.0       # optional — needed only if LLM_PROVIDER=anthropic
google-genai>=0.1.2     # optional — needed only if LLM_PROVIDER=gemini

# Environment / config
python-dotenv>=1.0.0

# Data generation (used by generate_sample_data.py)
faker>=24.0.0

# Jupyter / notebooks
jupyter>=1.0.0
ipykernel>=6.29.0
nbformat>=5.9.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

pycountry==26.2.16
